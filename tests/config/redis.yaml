apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: dapr-redis-master
  namespace: dapr-tests
spec:
  selector:
    matchLabels:
      app: redis
      role: master
      tier: backend
  serviceName: dapr-redis-headless
  template:
    metadata:
      labels:
        app: redis
        role: master
        tier: backend
    spec:
      containers:
      - name: master
        image: redis
        resources:
          requests:
            cpu: 100m
            memory: 100Mi
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: dapr-redis-master
  namespace: dapr-tests
  labels:
    app: redis
    role: master
    tier: backend
spec:
  ports:
  - port: 6379
    targetPort: 6379
  selector:
    app: redis
    role: master
    tier: backend
---
apiVersion: v1
kind: Service
metadata:
  name: dapr-redis-headless
  namespace: dapr-tests
  labels:
    app: redis
    role: master
    tier: backend
spec:
  clusterIP: None
  ports:
  - port: 6379
    targetPort: 6379
  selector:
    app: redis
    role: master
    tier: backend
